services:
  api:
    build: .
    container_name: gnosis-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: development
      PORT: 3000
      APP_VERSION: local-docker
